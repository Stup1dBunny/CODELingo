window.registerTheme({
  theme: 'TypeScript',
  id: 2,
  questions: [
    // ================= ТИПЫ ДАННЫХ В TS =================
    {
      id: 'ts-types-1',
      subtopic: 'Типы данных в TS',
      level: 'Junior-',
      q: 'Назови специфичные для TypeScript типы.',
      a: 'unknown, any, void, never, this. Плюс enum, tuple, literal types. Остальные типы (string, number, boolean, object, null, undefined, symbol, bigint) пришли из JS.',
      explain: 'Специфичные типы — те, которых нет в JS. any и unknown описывают неизвестное значение, void — отсутствие возврата, never — недостижимость.\n\n```ts\nlet u: unknown;\nlet n: never;\nlet v: void;\ntype T = [string, number]; // tuple\n```\n\nТипичная ошибка — считать any и unknown одним и тем же.',
      wrong: [
        'Только any и unknown. Остальные типы взяты из JavaScript.',
        'Только void и never — остальные есть в JavaScript.',
        'Только enum и tuple — остальные из JavaScript.'
      ]
    },
    {
      id: 'ts-types-2',
      subtopic: 'Типы данных в TS',
      level: 'Junior-',
      q: 'Что такое тип unknown?',
      a: 'Тип для значения, которое мы ещё не знаем. В отличие от any, требует проверки типа перед использованием — нельзя просто вызвать метод или обратиться к свойству.',
      explain: 'unknown — безопасная альтернатива any. Пока тип не сужен, TS не даст ничего с ним сделать.\n\n```ts\nfunction f(x: unknown) {\n  if (typeof x === "string") x.toUpperCase();\n}\n```\n\nВ отличие от any, unknown заставляет проверять тип.',
      wrong: [
        'Синоним any — то же самое, только короче.',
        'Тип для значений, которые никогда не возвращаются.',
        'Тип для функций без возвращаемого значения.'
      ]
    },
    {
      id: 'ts-types-3',
      subtopic: 'Типы данных в TS',
      level: 'Junior-',
      q: 'Что такое тип any?',
      a: 'Отключает типизацию для значения. С ним можно делать что угодно — TS не будет проверять. Использовать стоит только в крайних случаях: миграция с JS, сложные динамические API.',
      explain: 'any отключает проверку и автодополнение. Он совместим с любым типом в обе стороны, поэтому «заражает» вывод.\n\n```ts\nlet x: any = 5;\nx.toUpperCase(); // ошибки нет, но упадёт в рантайме\n```\n\nТипичная ошибка — использовать any вместо unknown с сужением.',
      wrong: [
        'Тип для значений, которые нужно проверить перед использованием.',
        'Синоним unknown, но безопаснее.',
        'Тип для чисел с плавающей точкой.'
      ]
    },
    {
      id: 'ts-types-4',
      subtopic: 'Типы данных в TS',
      level: 'Junior-',
      q: 'Что такое тип void?',
      a: 'Обозначает отсутствие возвращаемого значения. Используется для функций, которые ничего не возвращают (или возвращают undefined).',
      explain: 'void не то же самое, что never: функция с void завершается нормально, просто без значения. Функция с never не завершается.\n\n```ts\nfunction log(msg: string): void { console.log(msg); }\n```\n\nТипичная ошибка — путать void и never.',
      wrong: [
        'Тип для значений, которые никогда не возвращаются.',
        'Тип для пустых объектов.',
        'Синоним null.'
      ]
    },
    {
      id: 'ts-types-5',
      subtopic: 'Типы данных в TS',
      level: 'Junior-',
      q: 'Что такое тип never?',
      a: 'Тип для значений, которых не может быть. Функция с never никогда не возвращает управление: бросает исключение или зацикливается навсегда. Также never — нижний тип в иерархии.',
      explain: 'never совместим со всем, но ничто не совместимо с never. Используется для исчерпывающих проверок в switch.\n\n```ts\nfunction fail(msg: string): never { throw new Error(msg); }\n```\n\nТипичная ошибка — путать never и void.',
      wrong: [
        'Тип для функций без возвращаемого значения.',
        'Синоним void.',
        'Тип для значений, которые могут быть null.'
      ]
    },
    {
      id: 'ts-types-6',
      subtopic: 'Типы данных в TS',
      level: 'Junior-',
      q: 'Что такое enum?',
      a: 'Структура для создания набора именованных констант. По умолчанию значения — числа начиная с 0, но можно задавать строки.',
      explain: 'enum существует в рантайме как объект. Числовые enum имеют обратный маппинг, строковые — нет.\n\n```ts\nenum Status { Ok = "OK", Error = "ERROR" }\n```\n\nТипичная ошибка — использовать числовые enum без необходимости.',
      wrong: [
        'Массив строковых констант.',
        'Объект с readonly-свойствами.',
        'Тип для перечисления любых значений.'
      ]
    },
    {
      id: 'ts-types-7',
      subtopic: 'Типы данных в TS',
      level: 'Middle',
      q: 'В чём отличие any от unknown?',
      a: 'any полностью отключает проверку типов. unknown безопаснее: требует сужения типа (проверки) перед использованием. unknown — верхний тип, any — «дыра» в типизации.',
      explain: 'unknown можно присвоить any, но не наоборот без проверки. Это делает unknown безопасной точкой входа для внешних данных.\n\n```ts\nlet u: unknown = 5;\nlet a: any = u;\n// let s: string = u; // ошибка\n```\n\nТипичная ошибка — считать unknown неудобным и заменять на any.',
      wrong: [
        'any безопаснее, unknown опаснее.',
        'Это одно и то же.',
        'any работает только с примитивами, unknown — со всеми типами.'
      ]
    },
    {
      id: 'ts-types-8',
      subtopic: 'Типы данных в TS',
      level: 'Middle',
      q: 'Чем кортеж (tuple) отличается от массива?',
      a: 'Кортеж имеет фиксированную длину и фиксированные типы для каждой позиции. Массив — переменную длину с одним типом элементов.',
      explain: 'Кортеж удобен для пар и фиксированных наборов. Можно комбинировать с rest.\n\n```ts\ntype Pair = [string, number];\nconst p: Pair = ["age", 30];\n```\n\nТипичная ошибка — использовать tuple там, где достаточно массива.',
      wrong: [
        'Кортеж — это readonly-массив.',
        'Массив имеет фиксированную длину, кортеж — переменную.',
        'Отличий нет, это синонимы.'
      ]
    },
    {
      id: 'ts-types-9',
      subtopic: 'Типы данных в TS',
      level: 'Middle+',
      q: 'Что такое условные типы (conditional types)?',
      a: 'Типы, выбираемые в зависимости от условия: type Example = Dog extends Animal ? number : string. Позволяют строить типы на основе других типов.',
      explain: 'Условные типы — основа utility types (Exclude, Extract, ReturnType). В сочетании с infer извлекают типы.\n\n```ts\ntype IsString<T> = T extends string ? true : false;\n```\n\nТипичная ошибка — ожидать, что условие работает в рантайме.',
      wrong: [
        'Типы, которые проверяются во время выполнения.',
        'Типы с оператором if внутри interface.',
        'Синоним union-типов.'
      ]
    },
    {
      id: 'ts-types-10',
      subtopic: 'Типы данных в TS',
      level: 'Middle+',
      q: 'Когда уместно использовать тип any?',
      a: 'При миграции JS-проекта на TS, для работы со сторонними библиотеками без типов, для сложных динамических структур. Но лучше использовать unknown с проверками.',
      explain: 'any допустим как временная мера при миграции. В новом коде лучше unknown, дженерики или точные типы.\n\n```ts\n// временно при миграции\ndeclare const legacy: any;\n```\n\nТипичная ошибка — оставлять any в проекте навсегда.',
      wrong: [
        'Всегда, когда лень писать типы.',
        'Только для чисел.',
        'any использовать нельзя нигде — это устаревший тип.'
      ]
    },
    {
      id: 'ts-types-11',
      subtopic: 'Типы данных в TS',
      level: 'Senior',
      q: 'Какая иерархия типов в TypeScript?',
      a: 'any и unknown — верхние типы (от них наследуются все). never — нижний тип (наследуется от всех). Между ними — все остальные типы.',
      explain: 'Верхний тип совместим со всеми, нижний совместим со всем. never можно присвоить любому типу, любой тип можно присвоить unknown.\n\n```ts\nlet u: unknown = 5;\nlet n: never = fail();\nlet x: string = n; // ок\n```\n\nТипичная ошибка — путать верх и низ иерархии.',
      wrong: [
        'object — верхний тип, остальные наследуются от него.',
        'number — верхний, string — нижний.',
        'Иерархии типов в TS нет.'
      ]
    },
    {
      id: 'ts-types-12',
      subtopic: 'Типы данных в TS',
      level: 'Senior',
      q: 'Чем TS-типы отличаются от JS-типов?',
      a: 'TS-типы существуют только на этапе компиляции и стираются. JS-типы (typeof) существуют в рантайме. TS добавляет статическую проверку типов поверх динамического JS.',
      explain: 'После компиляции от типов не остаётся следа. Поэтому runtime-валидация (zod, io-ts) нужна для внешних данных.\n\n```ts\ntype User = { name: string };\n// в JS останется только код, тип исчезнет\n```\n\nТипичная ошибка — полагаться на TS-типы для данных из API.',
      wrong: [
        'TS-типы существуют в рантайме и влияют на поведение.',
        'JS-типы проверяются компилятором.',
        'Отличий нет — TS использует JS-типы.'
      ]
    },
    {
      id: 'ts-types-13',
      subtopic: 'Типы данных в TS',
      level: 'Senior',
      q: 'Является ли TypeScript системой структурной типизации?',
      a: 'Да. TS использует структурную типизацию: два типа совместимы, если у них совпадает структура, даже если имена разные. Номинальная типизация (как в Java/C#) не поддерживается, кроме private/protected полей.',
      explain: 'Структурная типизация позволяет передавать объект с лишними полями туда, где ожидается меньше. Приватные поля делают типы номинальными.\n\n```ts\ninterface A { x: number }\nconst b = { x: 1, y: 2 };\nconst a: A = b; // ок\n```\n\nТипичная ошибка — ожидать ошибку при разных именах типов.',
      wrong: [
        'Нет, TS использует номинальную типизацию, как Java.',
        'TS использует утиную типизацию только для объектов.',
        'TS вообще не проверяет структуру типов.'
      ]
    },
    {
      id: 'ts-types-14',
      subtopic: 'Типы данных в TS',
      level: 'Senior',
      q: 'Что такое номинальная типизация?',
      a: 'Система, где совместимость типов определяется именем, а не структурой. В TS эмулируется через брендированные типы (branded types): type UserId = string & { __brand: "UserId" }.',
      explain: 'Брендирование не даёт случайно передать обычную строку туда, где нужен UserId. Это частый приём в больших кодовых базах.\n\n```ts\ntype UserId = string & { __brand: "UserId" };\nfunction byId(id: UserId) {}\n```\n\nТипичная ошибка — думать, что TS не умеет номинальность вообще.',
      wrong: [
        'Система, где типы совместимы по структуре.',
        'Система, где типы совместимы по значениям.',
        'Система, где типы вообще не проверяются.'
      ]
    },

    // ================= ENUM =================
    {
      id: 'ts-enum-1',
      subtopic: 'Enum',
      level: 'Junior',
      q: 'Что такое enum и чем можно его заменить?',
      a: 'Enum — структура для набора именованных констант. Заменить можно через const-объект с as const: const Direction = { Up: 0, Down: 1 } as const;',
      explain: 'as const делает объект readonly и выводит literal-типы. Это современная альтернатива enum, не создающая рантайм-кода.\n\n```ts\nconst Direction = { Up: 0, Down: 1 } as const;\ntype Direction = typeof Direction[keyof typeof Direction];\n```\n\nТипичная ошибка — использовать enum там, где хватает union строк.',
      wrong: [
        'Заменить можно только другим enum.',
        'Заменить можно массивом строк.',
        'Enum ничем не заменить — это уникальный тип.'
      ]
    },
    {
      id: 'ts-enum-2',
      subtopic: 'Enum',
      level: 'Junior',
      q: 'Пример использования enum.',
      a: 'enum Direction { Up, Down, Left, Right } — значения 0, 1, 2, 3. Можно задавать свои: enum Status { Ok = "OK", Error = "ERROR" }.',
      explain: 'Строковые enum безопаснее числовых: нельзя передать произвольную строку. Числовые enum совместимы с любым числом.\n\n```ts\nenum Status { Ok = "OK", Error = "ERROR" }\nfunction set(s: Status) {}\n```\n\nТипичная ошибка — использовать числовые enum без явных значений.',
      wrong: [
        'enum Direction = ["Up", "Down"] — массив.',
        'enum Direction { Up: 0, Down: 1 } — как объект.',
        'enum Direction() { Up, Down } — со скобками.'
      ]
    },
    {
      id: 'ts-enum-3',
      subtopic: 'Enum',
      level: 'Middle',
      q: 'Во что транспилируется enum?',
      a: 'В объект, где каждому ключу соответствует значение, плюс обратный маппинг для числовых значений (obj[0] = "Up"). Для строковых enum обратного маппинга нет.',
      explain: 'Обратный маппинг удваивает размер объекта. Для строковых enum его нет.\n\n```js\nvar Direction;\n(function (Direction) {\n  Direction[Direction["Up"] = 0] = "Up";\n})(Direction || (Direction = {}));\n```\n\nТипичная ошибка — ожидать обратный маппинг у строкового enum.',
      wrong: [
        'В массив значений.',
        'В строку с перечислением.',
        'Тип enum стирается полностью без следа.'
      ]
    },
    {
      id: 'ts-enum-4',
      subtopic: 'Enum',
      level: 'Middle+',
      q: 'В чём проблема числовых enum?',
      a: 'Любое число совместимо с числовым enum. Например, функцию, принимающую Direction, можно вызвать с 99 — TS не пожалуется. Лучше использовать строковые enum.',
      explain: 'Это следствие структурной типизации: числовой enum — подтип number. Строковые enum не имеют такого недостатка.\n\n```ts\nenum D { Up, Down }\nfunction go(d: D) {}\ngo(99); // ошибки нет\n```\n\nТипичная ошибка — полагаться на числовые enum для валидации.',
      wrong: [
        'Числовые enum не поддерживаются в TS.',
        'Числовые enum медленнее строковых.',
        'Числовые enum нельзя транспилировать.'
      ]
    },
    {
      id: 'ts-enum-5',
      subtopic: 'Enum',
      level: 'Middle+',
      q: 'В чём разница между enum и const enum?',
      a: 'enum создаёт объект в рантайме, значения доступны динамически. const enum инлайнится при компиляции — объекта нет, производительность выше, но нет рантайм-представления.',
      explain: 'const enum подставляет значения прямо в код. Это экономит байты, но ломает динамический доступ.\n\n```ts\nconst enum Status { Ok = "OK" }\nconst s = Status.Ok; // скомпилируется в "OK"\n```\n\nТипичная ошибка — использовать const enum с isolatedModules.',
      wrong: [
        'const enum — то же самое, но короче.',
        'enum инлайнится, const enum создаёт объект.',
        'Отличий нет.'
      ]
    },
    {
      id: 'ts-enum-6',
      subtopic: 'Enum',
      level: 'Senior',
      q: 'В чём ограничение const enum?',
      a: 'Значения инлайнятся, но объект в рантайме отсутствует. Проблемы: нельзя использовать в динамических конструкциях, есть ограничения при isolatedModules и при реэкспорте в другие модули.',
      explain: 'При isolatedModules каждый файл компилируется отдельно, поэтому const enum из другого модуля недоступен. Babel и esbuild не поддерживают const enum по этой причине.\n\n```ts\n// import { Status } from "./status";\n// const s = Status.Ok; // может не сработать\n```\n\nТипичная ошибка — использовать const enum в библиотеках.',
      wrong: [
        'const enum работает только со строковыми значениями.',
        'const enum нельзя транспилировать.',
        'const enum поддерживается только в Node.js.'
      ]
    },

    // ================= ТИПИЗАЦИЯ ФУНКЦИЙ =================
    {
      id: 'ts-fn-1',
      subtopic: 'Типизация функций',
      level: 'Junior-',
      q: 'Как типизировать функцию?',
      a: 'Указываем типы параметров и возвращаемого значения: function sum(a: number, b: number): number { return a + b; }.',
      explain: 'Тип возврата можно не указывать — TS выведет его. Но явный тип полезен для публичных API.\n\n```ts\nfunction sum(a: number, b: number): number { return a + b; }\n```\n\nТипичная ошибка — указывать any для параметров.',
      wrong: [
        'Только тип возвращаемого значения.',
        'Только типы параметров.',
        'TS выводит типы автоматически — указывать не нужно.'
      ]
    },
    {
      id: 'ts-fn-2',
      subtopic: 'Типизация функций',
      level: 'Junior',
      q: 'Как указать опциональный аргумент функции?',
      a: 'Через знак вопроса: function sum(a?: number, b: number): number. Опциональные аргументы должны идти после обязательных.',
      explain: 'Опциональный параметр имеет тип number | undefined. Значение по умолчанию тоже делает параметр опциональным.\n\n```ts\nfunction greet(name = "world") {}\n```\n\nТипичная ошибка — ставить опциональный параметр перед обязательным.',
      wrong: [
        'Через ключевое слово optional.',
        'Через значение по умолчанию — других способов нет.',
        'Через null: a: number | null.'
      ]
    },
    {
      id: 'ts-fn-3',
      subtopic: 'Типизация функций',
      level: 'Junior',
      q: 'Как указать возвращаемое значение функции?',
      a: 'Через двоеточие после скобок: function f(): number { return 1; }. Если функция ничего не возвращает — void.',
      explain: 'Явный возвращаемый тип помогает поймать ошибки, когда функция возвращает не то. TS выведет тип, но явный надёжнее.\n\n```ts\nfunction f(): number { return 1; }\n```\n\nТипичная ошибка — указывать void там, где функция возвращает значение.',
      wrong: [
        'Через ключевое слово returns.',
        'Через отдельный декоратор @return.',
        'TS выводит сам, указать нельзя.'
      ]
    },
    {
      id: 'ts-fn-4',
      subtopic: 'Типизация функций',
      level: 'Junior',
      q: 'Как описать функцию типом?',
      a: 'type Fn = (a: number, b: number) => number; Или через interface: interface Fn { (a: number, b: number): number; }',
      explain: 'Стрелочная запись короче, интерфейс удобен для перегрузок. Оба варианта эквивалентны.\n\n```ts\ntype Fn = (a: number, b: number) => number;\n```\n\nТипичная ошибка — писать type Fn = function(...).',
      wrong: [
        'type Fn = function(a, b): number;',
        'type Fn = (number, number): number;',
        'Через type можно описать только объект.'
      ]
    },
    {
      id: 'ts-fn-5',
      subtopic: 'Типизация функций',
      level: 'Middle',
      q: 'Как типизировать функцию с неограниченным числом аргументов?',
      a: 'Через rest-параметр: function sum(...args: number[]): number { ... }. Если типы разные — any[] или unknown[].',
      explain: 'Rest-параметр должен быть последним. Он собирает все оставшиеся аргументы в массив.\n\n```ts\nfunction sum(...args: number[]): number { return args.reduce((a, b) => a + b, 0); }\n```\n\nТипичная ошибка — использовать arguments вместо rest.',
      wrong: [
        'Через arguments: any[].',
        'Через специальный тип Rest.',
        'Функции с переменным числом аргументов нельзя типизировать.'
      ]
    },
    {
      id: 'ts-fn-6',
      subtopic: 'Типизация функций',
      level: 'Senior',
      q: 'Что такое перегрузка функций (overload)?',
      a: 'Несколько сигнатур для одной функции с разными типами аргументов. Реализация одна, но TS проверяет вызовы по сигнатурам.',
      explain: 'Полезно, когда функция возвращает разный тип в зависимости от аргументов. Реализация должна быть совместима со всеми сигнатурами.\n\n```ts\nfunction f(x: string): string;\nfunction f(x: number): number;\nfunction f(x: any): any { return x; }\n```\n\nТипичная ошибка — писать слишком много перегрузок вместо union.',
      wrong: [
        'Определение одной функции с одним набором типов.',
        'Переопределение функции в дочернем классе.',
        'Автоматический вывод типов.'
      ]
    },
    {
      id: 'ts-fn-7',
      subtopic: 'Типизация функций',
      level: 'Senior',
      q: 'Во что транспилируется перегрузка функций?',
      a: 'Ни во что особенное — в JS остаётся одна функция. Проверка перегрузок существует только на этапе компиляции.',
      explain: 'В рантайме перегрузок нет: остаётся последняя реализация. Поэтому внутри функции нужно самому разбирать типы аргументов.\n\n```js\n// после компиляции\nfunction f(x) { return x; }\n```\n\nТипичная ошибка — ожидать разного поведения в рантайме.',
      wrong: [
        'В несколько функций с разными именами.',
        'В switch по типам аргументов.',
        'В отдельный модуль с перегрузками.'
      ]
    },

    // ================= UNION / INTERSECTION =================
    {
      id: 'ts-union-1',
      subtopic: 'Типы — Union, Intersection',
      level: 'Junior',
      q: 'Что такое Union-тип?',
      a: 'Тип, объединяющий несколько: let x: number | string. Переменная может быть одного из указанных типов.',
      explain: 'Union читается как «или». Перед использованием специфичных методов нужно сузить тип.\n\n```ts\nfunction f(x: number | string) {\n  if (typeof x === "string") x.toUpperCase();\n}\n```\n\nТипичная ошибка — вызывать метод, который есть только у одного члена union.',
      wrong: [
        'Тип, требующий соответствия всем указанным типам.',
        'Тип, объединяющий объекты через &',
        'Синоним any.'
      ]
    },
    {
      id: 'ts-union-2',
      subtopic: 'Типы — Union, Intersection',
      level: 'Junior+',
      q: 'Что такое Intersection-тип?',
      a: 'Тип, требующий соответствия всем указанным типам одновременно: type A = B & C. Объект должен иметь поля и из B, и из C.',
      explain: 'Intersection читается как «и». Часто используется для объединения интерфейсов и миксинов.\n\n```ts\ntype A = { x: number } & { y: string };\nconst a: A = { x: 1, y: "s" };\n```\n\nТипичная ошибка — путать & и |.',
      wrong: [
        'Тип, объединяющий несколько через |',
        'Синоним Union.',
        'Тип, который выбирает один из нескольких.'
      ]
    },
    {
      id: 'ts-union-3',
      subtopic: 'Типы — Union, Intersection',
      level: 'Junior+',
      q: 'Как указать диапазон типов для переменной?',
      a: 'Через union: let title: number | string | boolean.',
      explain: 'Union перечисляет допустимые типы. Это не диапазон значений, а именно набор типов.\n\n```ts\nlet id: string | number;\n```\n\nТипичная ошибка — думать, что union задаёт диапазон чисел.',
      wrong: [
        'Через range: number..string.',
        'Через enum.',
        'Только через any — других способов нет.'
      ]
    },
    {
      id: 'ts-union-4',
      subtopic: 'Типы — Union, Intersection',
      level: 'Middle',
      q: 'Чем в TypeScript можно заменить наследование для типов?',
      a: 'Через интерфейсы с extends, пересечения (&) и union (|). Композиция типов вместо наследования.',
      explain: 'Композиция через & позволяет собрать тип из частей. Это гибче, чем жёсткая иерархия.\n\n```ts\ntype WithId = { id: string };\ntype User = WithId & { name: string };\n```\n\nТипичная ошибка — строить глубокие иерархии вместо композиции.',
      wrong: [
        'Только через interface extends.',
        'Только через class extends.',
        'Наследование типов заменить нельзя.'
      ]
    },
    {
      id: 'ts-union-5',
      subtopic: 'Типы — Union, Intersection',
      level: 'Senior',
      q: 'Как отличить, какой тип из union используется?',
      a: 'Через type guards: typeof, instanceof, in, проверку на null, дискриминированные union (по общему полю-тегу).',
      explain: 'Дискриминированный union — самый надёжный способ: общее поле-тег однозначно указывает на вариант.\n\n```ts\ntype Shape =\n  | { kind: "circle"; r: number }\n  | { kind: "square"; side: number };\n```\n\nТипичная ошибка — использовать as вместо сужения.',
      wrong: [
        'Через оператор union.',
        'Через приведение типа as.',
        'Различить нельзя — TS сам определяет.'
      ]
    },

    // ================= UTILITY TYPES =================
    {
      id: 'ts-util-1',
      subtopic: 'Utility Types',
      level: 'Junior+',
      q: 'Что делает Readonly<T>?',
      a: 'Делает все свойства типа T только для чтения. Попытка изменить — ошибка компиляции.',
      explain: 'Readonly поверхностный: вложенные объекты остаются изменяемыми. Для глубокого readonly нужен свой mapped type.\n\n```ts\ntype R = Readonly<{ x: number }>;\nconst r: R = { x: 1 };\n// r.x = 2; // ошибка\n```\n\nТипичная ошибка — ожидать глубокой заморозки.',
      wrong: [
        'Делает все свойства необязательными.',
        'Делает все свойства обязательными.',
        'Удаляет указанные свойства.'
      ]
    },
    {
      id: 'ts-util-2',
      subtopic: 'Utility Types',
      level: 'Junior+',
      q: 'Что делает Partial<T>?',
      a: 'Делает все свойства типа T необязательными (добавляет ?). Полезно для обновления объекта по частям.',
      explain: 'Partial часто используется в функциях обновления: принимает только изменяемые поля.\n\n```ts\nfunction update(user: User, patch: Partial<User>) {}\n```\n\nТипичная ошибка — использовать Partial там, где нужен Pick.',
      wrong: [
        'Делает все свойства обязательными.',
        'Делает все свойства readonly.',
        'Выбирает подмножество свойств.'
      ]
    },
    {
      id: 'ts-util-3',
      subtopic: 'Utility Types',
      level: 'Junior+',
      q: 'Что делает Required<T>?',
      a: 'Делает все свойства типа T обязательными (убирает ?).',
      explain: 'Обратный Partial. Полезен, когда после валидации все поля точно есть.\n\n```ts\ntype Full = Required<Partial<User>>;\n```\n\nТипичная ошибка — путать Required и Readonly.',
      wrong: [
        'Делает все свойства необязательными.',
        'Делает все свойства readonly.',
        'Удаляет указанные свойства.'
      ]
    },
    {
      id: 'ts-util-4',
      subtopic: 'Utility Types',
      level: 'Middle',
      q: 'Что делает Pick<Type, Keys>?',
      a: 'Создаёт тип с выбранными свойствами Keys из Type. Пример: Pick<User, "id" | "name">.',
      explain: 'Pick оставляет только указанные поля. Это удобно для DTO и публичных представлений.\n\n```ts\ntype UserPreview = Pick<User, "id" | "name">;\n```\n\nТипичная ошибка — путать Pick и Omit.',
      wrong: [
        'Удаляет указанные свойства.',
        'Делает свойства необязательными.',
        'Делает свойства readonly.'
      ]
    },
    {
      id: 'ts-util-5',
      subtopic: 'Utility Types',
      level: 'Middle',
      q: 'Что делает Omit<Type, Keys>?',
      a: 'Создаёт тип с исключёнными свойствами Keys из Type. Обратный Pick. Пример: Omit<User, "password">.',
      explain: 'Omit удобен для скрытия чувствительных полей. Работает как Pick с Exclude по ключам.\n\n```ts\ntype PublicUser = Omit<User, "password">;\n```\n\nТипичная ошибка — путать Omit и Exclude (один для объектов, другой для union).',
      wrong: [
        'Выбирает указанные свойства.',
        'Делает свойства обязательными.',
        'Делает свойства readonly.'
      ]
    },
    {
      id: 'ts-util-6',
      subtopic: 'Utility Types',
      level: 'Middle',
      q: 'Что делает Record<Keys, Type>?',
      a: 'Создаёт тип объекта, где каждый ключ из Keys соответствует значению Type. Пример: Record<string, number>.',
      explain: 'Record удобен для словарей и карт. Ключи могут быть union строк или number/symbol.\n\n```ts\nconst scores: Record<string, number> = { math: 5 };\n```\n\nТипичная ошибка — использовать Record там, где нужен интерфейс с фиксированными полями.',
      wrong: [
        'Создаёт массив значений Type.',
        'Создаёт Map с ключами Keys.',
        'Создаёт тип с обязательными свойствами Keys.'
      ]
    },
    {
      id: 'ts-util-7',
      subtopic: 'Utility Types',
      level: 'Middle',
      q: 'Что делает Exclude<Type, ExcludedUnion>?',
      a: 'Исключает из Type те члены union, которые совпадают с ExcludedUnion. Пример: Exclude<"a" | "b" | "c", "a"> = "b" | "c".',
      explain: 'Exclude работает с union-типами, а не с объектами. Для объектов есть Omit.\n\n```ts\ntype T = Exclude<"a" | "b", "a">; // "b"\n```\n\nТипичная ошибка — применять Exclude к объекту.',
      wrong: [
        'Выбирает только совпадающие члены.',
        'Удаляет свойства объекта.',
        'Делает тип необязательным.'
      ]
    },
    {
      id: 'ts-util-8',
      subtopic: 'Utility Types',
      level: 'Middle+',
      q: 'Что делает ReturnType<T>?',
      a: 'Извлекает тип возвращаемого значения функции T. Пример: type R = ReturnType<() => string>; // string.',
      explain: 'ReturnType использует infer внутри условного типа. Работает только с функциями.\n\n```ts\nfunction f() { return { x: 1 }; }\ntype R = ReturnType<typeof f>; // { x: number }\n```\n\nТипичная ошибка — применять ReturnType к не-функции.',
      wrong: [
        'Возвращает тип аргументов функции.',
        'Возвращает саму функцию.',
        'Возвращает тип this функции.'
      ]
    },
    {
      id: 'ts-util-9',
      subtopic: 'Utility Types',
      level: 'Senior',
      q: 'Что такое Mapped Types?',
      a: 'Типы, которые преобразуют свойства другого типа: type Partial<T> = { [K in keyof T]?: T[K] }. Позволяют массово менять модификаторы свойств.',
      explain: 'Mapped types — основа большинства utility types. С их помощью можно добавлять/убирать readonly и ?.\n\n```ts\ntype Nullable<T> = { [K in keyof T]: T[K] | null };\n```\n\nТипичная ошибка — путать mapped types с индексными сигнатурами.',
      wrong: [
        'Типы, которые хранят данные в Map.',
        'Типы, которые описывают функции.',
        'Синоним Union-типов.'
      ]
    },
    {
      id: 'ts-util-10',
      subtopic: 'Utility Types',
      level: 'Senior',
      q: 'Что делают префиксы + и - в Mapped Types?',
      a: 'Добавляют или убирают модификаторы readonly и ?. Пример: { -readonly [K in keyof T]: T[K] } убирает readonly.',
      explain: 'По умолчанию модификаторы сохраняются. + и - управляют ими явно.\n\n```ts\ntype Mutable<T> = { -readonly [K in keyof T]: T[K] };\n```\n\nТипичная ошибка — забыть, что без префикса модификатор сохраняется.',
      wrong: [
        'Плюс добавляет свойство, минус удаляет.',
        'Плюс делает обязательным, минус — необязательным (только для методов).',
        'Это просто синтаксис без смысла.'
      ]
    },

    // ================= GENERIC =================
    {
      id: 'ts-gen-1',
      subtopic: 'Generic',
      level: 'Middle',
      q: 'Что такое generic?',
      a: 'Инструмент для создания гибких переиспользуемых типов. Позволяет определять тип в момент использования. Пример: function identity<T>(x: T): T { return x; }',
      explain: 'Generic — параметр типа. Он сохраняет связь между аргументом и результатом, чего any не делает.\n\n```ts\nfunction first<T>(arr: T[]): T | undefined { return arr[0]; }\n```\n\nТипичная ошибка — заменять generic на any.',
      wrong: [
        'Тип для любых значений, как any.',
        'Универсальный интерфейс для всех типов.',
        'Абстрактный класс без реализации.'
      ]
    },
    {
      id: 'ts-gen-2',
      subtopic: 'Generic',
      level: 'Middle',
      q: 'Зачем нужен generic?',
      a: 'Позволяет писать функции, классы и типы, работающие с любым типом, сохраняя проверку типов. Без generic пришлось бы использовать any и терять типизацию.',
      explain: 'Generic сохраняет информацию о типе. Например, useState<T> возвращает [T, setter], а не [any, any].\n\n```ts\nfunction wrap<T>(v: T): { value: T } { return { value: v }; }\n```\n\nТипичная ошибка — использовать any вместо generic.',
      wrong: [
        'Для отключения типизации.',
        'Для создания перегрузок.',
        'Для ускорения компиляции.'
      ]
    },
    {
      id: 'ts-gen-3',
      subtopic: 'Generic',
      level: 'Middle',
      q: 'Как задать generic параметр по умолчанию?',
      a: 'function createPair<K = string, V = number>(key: K, value: V): [K, V]. Если тип не указан — используются значения по умолчанию.',
      explain: 'Значение по умолчанию используется, когда тип не выведен и не указан явно. Это похоже на параметры функций.\n\n```ts\nfunction createPair<K = string, V = number>(k: K, v: V): [K, V] { return [k, v]; }\n```\n\nТипичная ошибка — ставить generic без default перед generic с default.',
      wrong: [
        'function createPair<K: string, V: number>(...)',
        'function createPair<K?, V?>(...)',
        'Параметры по умолчанию в generic невозможны.'
      ]
    },
    {
      id: 'ts-gen-4',
      subtopic: 'Generic',
      level: 'Middle+',
      q: 'Для чего используется extends в generic?',
      a: 'Для ограничения generic-параметра: <T extends Type> — T должен быть совместим с Type. Позволяет использовать свойства Type внутри функции.',
      explain: 'extends в generic — это ограничение, а не наследование класса. Внутри функции можно обращаться к полям Type.\n\n```ts\nfunction getLength<T extends { length: number }>(x: T) { return x.length; }\n```\n\nТипичная ошибка — путать ограничение generic с наследованием.',
      wrong: [
        'Для наследования класса.',
        'Для указания значения по умолчанию.',
        'Для расширения интерфейса.'
      ]
    },
    {
      id: 'ts-gen-5',
      subtopic: 'Generic',
      level: 'Middle+',
      q: 'Можно ли вложить generic в generic?',
      a: 'Да: function f<T extends Array<U>, U>(...) или type Wrapper<T> = { value: T; next: Wrapper<T> | null }.',
      explain: 'Вложенные generic позволяют описывать рекурсивные структуры (деревья, списки) и связанные типы.\n\n```ts\ntype Tree<T> = { value: T; children: Tree<T>[] };\n```\n\nТипичная ошибка — забыть ограничение и получить слишком широкий тип.',
      wrong: [
        'Нет, только один уровень вложенности.',
        'Только для функций, для типов нельзя.',
        'Только через отдельные псевдонимы.'
      ]
    },
    {
      id: 'ts-gen-6',
      subtopic: 'Generic',
      level: 'Senior',
      q: 'Как написать generic для класса?',
      a: 'class Box<T> { constructor(public value: T) {} getValue(): T { return this.value; } }. При создании: new Box<number>(5).',
      explain: 'Generic класса сохраняет тип на всех методах. Можно комбинировать с ограничениями.\n\n```ts\nclass Box<T> {\n  constructor(public value: T) {}\n  getValue(): T { return this.value; }\n}\n```\n\nТипичная ошибка — использовать any вместо generic в контейнерах.',
      wrong: [
        'class Box { value: any } — generic для классов невозможен.',
        'class Box<T> { value: T } — без конструктора.',
        'Только через интерфейс, не через класс.'
      ]
    },
    {
      id: 'ts-gen-7',
      subtopic: 'Generic',
      level: 'Senior',
      q: 'Когда generic особенно полезен?',
      a: 'Для контейнеров (Stack, Queue, Map), API-клиентов, утилит (map, filter), React-хуков (useState<T>), Redux-экшенов. Везде, где тип заранее неизвестен.',
      explain: 'Generic сохраняет типы в цепочках вызовов. Без него пришлось бы использовать any и терять подсказки.\n\n```ts\nfunction map<T, R>(arr: T[], fn: (x: T) => R): R[] { return arr.map(fn); }\n```\n\nТипичная ошибка — использовать generic для простых случаев без необходимости.',
      wrong: [
        'Только для массивов.',
        'Только для классов-коллекций.',
        'Только для React-компонентов.'
      ]
    },
    // ================= TYPE GUARDS =================
    {
      id: 'ts-guard-1',
      subtopic: 'Type Guards',
      level: 'Middle',
      q: 'Что такое Type Guards?',
      a: 'Механизмы сужения типа переменной до конкретного типа. Позволяют безопасно работать со свойствами, специфичными для узкого типа.',
      explain: 'Type guards работают на уровне компиляции: TS анализирует проверки и сужает тип внутри блока. В рантайме это обычные условия.\n\n```ts\nfunction f(x: string | number) {\n  if (typeof x === "string") x.toUpperCase();\n}\n```\n\nТипичная ошибка — использовать as вместо реальной проверки.',
      wrong: [
        'Проверки типов во время выполнения, бросающие ошибку.',
        'Декораторы для валидации.',
        'Обёртки над any.'
      ]
    },
    {
      id: 'ts-guard-2',
      subtopic: 'Type Guards',
      level: 'Middle',
      q: 'Для чего нужны операторы typeof, instanceof, keyof?',
      a: 'typeof — проверка примитивов. instanceof — проверка класса (объекта). keyof — получение ключей объекта в виде union строк.',
      explain: 'typeof и instanceof работают в рантайме и сужают типы. keyof — чисто типовой оператор, в рантайме его нет.\n\n```ts\ntype Keys = keyof { a: 1; b: 2 }; // "a" | "b"\n```\n\nТипичная ошибка — путать keyof с Object.keys.',
      wrong: [
        'typeof — для объектов, instanceof — для примитивов, keyof — для массивов.',
        'Все три делают одно и то же.',
        'keyof — оператор рантайма, typeof и instanceof — компиляции.'
      ]
    },
    {
      id: 'ts-guard-3',
      subtopic: 'Type Guards',
      level: 'Middle+',
      q: 'Как реализовать сужение диапазона типов к одному?',
      a: 'Через проверки: typeof x === "string", x instanceof Date, "prop" in x, x !== null, Array.isArray(x). TS автоматически сужает тип внутри блока if.',
      explain: 'Сужение работает только внутри ветки, где проверка истинна. После блока тип снова становится union.\n\n```ts\nif (Array.isArray(x)) x.push(1);\n```\n\nТипичная ошибка — проверять тип вне блока и терять сужение.',
      wrong: [
        'Через приведение as — это не сужение.',
        'Через typeof x — работает только для чисел.',
        'Сужение происходит автоматически без проверок.'
      ]
    },
    {
      id: 'ts-guard-4',
      subtopic: 'Type Guards',
      level: 'Middle+',
      q: 'Как работает сужение типов с оператором in?',
      a: 'if ("prop" in obj) — внутри блока TS знает, что у obj есть свойство prop, и сужает тип до одного из union-членов, содержащих это свойство.',
      explain: 'in полезен для дискриминированных union и объектов с разными наборами полей. Он работает и в рантайме.\n\n```ts\ntype A = { a: number } | { b: string };\nfunction f(x: A) { if ("a" in x) x.a; }\n```\n\nТипичная ошибка — использовать in для примитивов.',
      wrong: [
        'in проверяет только массивы.',
        'in не работает для сужения типов.',
        'in требует явного приведения as.'
      ]
    },
    {
      id: 'ts-guard-5',
      subtopic: 'Type Guards',
      level: 'Senior',
      q: 'Что такое пользовательские type guards?',
      a: 'Функции с предикатом is: function isString(x: unknown): x is string { return typeof x === "string"; }. TS использует их для сужения.',
      explain: 'Предикат is сообщает компилятору, что функция гарантирует тип. Это удобно для повторно используемых проверок.\n\n```ts\nfunction isString(x: unknown): x is string {\n  return typeof x === "string";\n}\n```\n\nТипичная ошибка — возвращать boolean вместо предиката is.',
      wrong: [
        'Функции, возвращающие boolean.',
        'Функции, бросающие ошибки при неверном типе.',
        'Декораторы для проверки типов.'
      ]
    },
    {
      id: 'ts-guard-6',
      subtopic: 'Type Guards',
      level: 'Senior',
      q: 'Что такое discriminated union?',
      a: 'Union типов с общим полем-тегом (например, type: "success" | "error"). По этому тегу TS сужает тип в switch/if.',
      explain: 'Тег должен быть literal-типом. В switch по тегу TS точно знает вариант и проверяет исчерпывающест�� через never.\n\n```ts\ntype Result =\n  | { status: "ok"; data: string }\n  | { status: "err"; error: Error };\n```\n\nТипичная ошибка — использовать не-literal тег.',
      wrong: [
        'Union, в котором все типы одинаковы.',
        'Union с любым полем.',
        'Синоним Intersection.'
      ]
    },

    // ================= КОНФИГУРАЦИЯ =================
    {
      id: 'ts-config-1',
      subtopic: 'Конфигурация',
      level: 'Junior',
      q: 'Что такое tsconfig.json?',
      a: 'Файл конфигурации TypeScript. Определяет, как компилятор обрабатывает проект: какие файлы включать, какие проверки включить, куда компилировать.',
      explain: 'tsconfig.json лежит в корне проекта. Компилятор ищет его вверх по дереву каталогов.\n\n```json\n{\n  "compilerOptions": { "strict": true },\n  "include": ["src"]\n}\n```\n\nТипичная ошибка — отсутствие tsconfig и потеря проверок.',
      wrong: [
        'Файл с типами проекта.',
        'Файл с настройками линтера.',
        'Файл с зависимостями проекта.'
      ]
    },
    {
      id: 'ts-config-2',
      subtopic: 'Конфигурация',
      level: 'Junior+',
      q: 'Что означает strictNullChecks?',
      a: 'Флаг для явной проверки null и undefined. При включении null и undefined не входят в другие типы автоматически — их нужно указывать через union.',
      explain: 'Без этого флага null и undefined совместимы со всеми типами, что скрывает ошибки. Входит в strict.\n\n```ts\nlet s: string = null; // ошибка при strictNullChecks\n```\n\nТипичная ошибка — отключать флаг ради «удобства».',
      wrong: [
        'Флаг для запрета null в проекте.',
        'Флаг для автоматического приведения null к undefined.',
        'Флаг для отключения проверки типов.'
      ]
    },
    {
      id: 'ts-config-3',
      subtopic: 'Конфигурация',
      level: 'Junior+',
      q: 'Что означает noImplicitAny?',
      a: 'Флаг, выдающий ошибку, если тип не может быть выведен явно, а any подставляется неявно. Заставляет явно указывать типы.',
      explain: 'Флаг не запрещает any — только неявный. Явный any остаётся допустимым.\n\n```ts\nfunction f(x) {} // ошибка при noImplicitAny\n```\n\nТипичная ошибка — путать noImplicitAny с полным запретом any.',
      wrong: [
        'Запрещает использование any.',
        'Автоматически расставляет типы.',
        'Отключает проверку типов.'
      ]
    },
    {
      id: 'ts-config-4',
      subtopic: 'Конфигурация',
      level: 'Junior+',
      q: 'Что означают include и exclude в tsconfig?',
      a: 'include — файлы/папки для компиляции. exclude — что исключить (например, node_modules, dist).',
      explain: 'По умолчанию компилируются все .ts файлы, кроме node_modules. include/exclude уточняют набор.\n\n```json\n{ "include": ["src"], "exclude": ["node_modules"] }\n```\n\nТипичная ошибка — забыть exclude и компилировать зависимости.',
      wrong: [
        'include — зависимости, exclude — devDependencies.',
        'include — типы, exclude — значения.',
        'Это алиасы для files и ignore.'
      ]
    },
    {
      id: 'ts-config-5',
      subtopic: 'Конфигурация',
      level: 'Middle',
      q: 'Для чего нужен флаг strict?',
      a: 'Включает сразу группу строгих флагов (strictNullChecks, noImplicitAny, strictFunctionTypes и др.). Рекомендуется для всех новых проектов.',
      explain: 'strict — это агрегатор, а не отдельная проверка. Можно включать флаги по отдельности, но проще strict.\n\n```json\n{ "compilerOptions": { "strict": true } }\n```\n\nТипичная ошибка — включать strict и не разбираться с ошибками.',
      wrong: [
        'Включает только strictNullChecks.',
        'Запрещает any.',
        'Ускоряет компиляцию.'
      ]
    },
    {
      id: 'ts-config-6',
      subtopic: 'Конфигурация',
      level: 'Senior',
      q: 'Что означает опция preserveConstEnums?',
      a: 'Сохраняет const enum как обычный enum в рантайме. Полезно для отладки и при isolatedModules, где const enum иначе инлайнится не полностью.',
      explain: 'По умолчанию const enum исчезает. С preserveConstEnums остаётся объект, как у обычного enum.\n\n```json\n{ "compilerOptions": { "preserveConstEnums": true } }\n```\n\nТипичная ошибка — использовать const enum в библиотеках при isolatedModules.',
      wrong: [
        'Запрещает const enum.',
        'Автоматически конвертирует enum в объект.',
        'Удаляет enum из сборки.'
      ]
    },

    // ================= ООП — МИКСИНЫ =================
    {
      id: 'ts-oop-1',
      subtopic: 'ООП — Миксины',
      level: 'Middle',
      q: 'Что такое миксины и чем отличаются от наследования?',
      a: 'Миксины позволяют объединять функциональность нескольких классов через композицию. Наследование — иерархия «родитель-потомок». Миксины дают множественное поведение без множественного наследования.',
      explain: 'Миксин — функция, принимающая класс и возвращающая расширенный класс. Это позволяет «подмешивать» поведение.\n\n```ts\ntype Ctor<T = {}> = new (...args: any[]) => T;\nfunction Serializable<T extends Ctor>(Base: T) {\n  return class extends Base { serialize() { return JSON.stringify(this); } };\n}\n```\n\nТипичная ошибка — путать миксины с множественным наследованием.',
      wrong: [
        'Миксины — это классы без реализации.',
        'Миксины — синоним наследования.',
        'Миксины — декораторы классов.'
      ]
    },
    {
      id: 'ts-oop-2',
      subtopic: 'ООП — Миксины',
      level: 'Senior',
      q: 'Как сделать миксин в TypeScript?',
      a: 'Через функцию, принимающую конструктор и возвращающую расширенный класс: type Constructor<T = {}> = new (...args: any[]) => T; function Serializable<T extends Constructor>(Base: T) { return class extends Base { serialize() { return JSON.stringify(this); } }; }',
      explain: 'Ограничение Constructor гарантирует, что Base — класс. Возвращаемый класс расширяет Base и добавляет методы.\n\n```ts\nclass User extends Serializable(class {}) {}\n```\n\nТипичная ошибка — забыть ограничение и получить ошибку extends.',
      wrong: [
        'Через class A extends B, C — множественное наследование.',
        'Через Object.assign к prototype.',
        'Через интерфейсы без реализации.'
      ]
    },

    // ================= ООП — ДЕКОРАТОРЫ =================
    {
      id: 'ts-oop-3',
      subtopic: 'ООП — Декораторы',
      level: 'Middle',
      q: 'Что такое декоратор?',
      a: 'Функция, которая модифицирует поведение классов, методов, свойств или параметров. Применяется через @decorator над объявлением.',
      explain: 'Декораторы — экспериментальная фича TS, включается через experimentalDecorators. В стандарте JS они пока на стадии提案.\n\n```ts\n@Component({ selector: "app" })\nclass App {}\n```\n\nТипичная ошибка — забыть включить experimentalDecorators.',
      wrong: [
        'Аннотация типа.',
        'Обёртка над функцией для логирования.',
        'Способ наследования классов.'
      ]
    },
    {
      id: 'ts-oop-4',
      subtopic: 'ООП — Декораторы',
      level: 'Middle+',
      q: 'Какие виды декораторов бывают?',
      a: 'Класса, метода, свойства, параметра, геттера/сеттера. Каждый получает свой набор аргументов (target, propertyKey, descriptor).',
      explain: 'Декоратор метода получает descriptor и может обернуть оригинальный метод. Декоратор свойства — только target и key.\n\n```ts\nfunction Log(target: any, key: string, desc: PropertyDescriptor) {\n  const orig = desc.value;\n  desc.value = (...args: any[]) => { console.log(key); return orig.apply(this, args); };\n}\n```\n\nТипичная ошибка — путать аргументы разных видов декораторов.',
      wrong: [
        'Только класса и метода.',
        'Только свойства и параметра.',
        'Только класса.'
      ]
    },
    {
      id: 'ts-oop-5',
      subtopic: 'ООП — Декораторы',
      level: 'Middle+',
      q: 'Чем отличается параметризированный декоратор от обычного?',
      a: 'Параметризированный — это функция, возвращающая декоратор: @Log("info") → Log("info") возвращает функцию-декоратор. Обычный декоратор применяется напрямую.',
      explain: 'Параметризированный декоратор — фабрика декораторов. Это позволяет передавать настройки.\n\n```ts\nfunction Log(level: string) {\n  return (target: any, key: string) => { /* ... */ };\n}\n```\n\nТипичная ошибка — забыть вызвать фабрику: @Log вместо @Log("info").',
      wrong: [
        'Параметризированный принимает параметры класса.',
        'Обычный не может принимать аргументы.',
        'Отличий нет.'
      ]
    },
    {
      id: 'ts-oop-6',
      subtopic: 'ООП — Декораторы',
      level: 'Senior',
      q: 'Что такое reflect-metadata и зачем нужно?',
      a: 'Полифил для Reflection API. Позволяет декораторам сохранять и читать метаданные о классах и их членах (типы параметров, свойства). Основа для DI-контейнеров (Angular, NestJS).',
      explain: 'Без reflect-metadata декоратор не знает типы параметров. Пакет добавляет Reflect.metadata и getMetadata.\n\n```ts\nReflect.defineMetadata("role", "admin", User);\nReflect.getMetadata("role", User); // "admin"\n```\n\nТипичная ошибка — использовать DI без reflect-metadata.',
      wrong: [
        'Библиотека для валидации.',
        'Альтернатива декораторам.',
        'Утилита для сериализации.'
      ]
    },

    // ================= ООП — МОДИФИКАТОРЫ ДОСТУПА =================
    {
      id: 'ts-oop-7',
      subtopic: 'ООП — Модификаторы доступа',
      level: 'Junior',
      q: 'Что такое модификаторы доступа?',
      a: 'Ключевые слова, ограничивающие доступ к свойствам и методам класса: public, private, protected, readonly.',
      explain: 'Модификаторы влияют только на проверку компилятором. В рантайме private не существует.\n\n```ts\nclass User {\n  private name: string = "";\n  public getName() { return this.name; }\n}\n```\n\nТипичная ошибка — думать, что private защищает в рантайме.',
      wrong: [
        'Способ наследования классов.',
        'Аннотации типов.',
        'Декораторы.'
      ]
    },
    {
      id: 'ts-oop-8',
      subtopic: 'ООП — Модификаторы доступа',
      level: 'Junior+',
      q: 'Для чего нужен public?',
      a: 'Делает свойство или метод доступным отовсюду. Это значение по умолчанию — можно не указывать.',
      explain: 'public — модификатор по умолчанию. Явное указание полезно для читаемости.\n\n```ts\nclass A { public x = 1; }\n```\n\nТипичная ошибка — писать public везде без необходимости.',
      wrong: [
        'Ограничивает доступ только классом.',
        'Делает свойство только для чтения.',
        'Делает метод статическим.'
      ]
    },
    {
      id: 'ts-oop-9',
      subtopic: 'ООП — Модификаторы доступа',
      level: 'Junior+',
      q: 'Для чего нужен private?',
      a: 'Делает свойство или метод доступным только внутри класса, где объявлен. Наследники и внешний код не имеют доступа.',
      explain: 'private — только внутри класса. Наследники не видят его, в отличие от protected.\n\n```ts\nclass A { private x = 1; }\n// new A().x; // ошибка\n```\n\nТипичная ошибка — путать private и protected.',
      wrong: [
        'Делает свойство доступным наследникам.',
        'Делает свойство readonly.',
        'Делает метод статическим.'
      ]
    },
    {
      id: 'ts-oop-10',
      subtopic: 'ООП — Модификаторы доступа',
      level: 'Junior+',
      q: 'Что такое readonly?',
      a: 'Модификатор, запрещающий изменение свойства после инициализации. Можно задать в конструкторе или при объявлении.',
      explain: 'readonly проверяется только компилятором. Массивы и объекты внутри остаются изменяемыми.\n\n```ts\nclass A { readonly id: string = "1"; }\n```\n\nТипичная ошибка — ожидать глубокой неизменяемости.',
      wrong: [
        'Модификатор, делающий свойство приватным.',
        'Модификатор, делающий свойство статическим.',
        'Модификатор, делающий свойство необязательным.'
      ]
    },
    {
      id: 'ts-oop-11',
      subtopic: 'ООП — Модификаторы доступа',
      level: 'Middle',
      q: 'Что такое инкапсуляция?',
      a: 'Принцип ООП: скрытие внутренней реализации объекта. Доступ к данным — только через публичный интерфейс. В TS достигается через private и protected.',
      explain: 'Инкапсуляция позволяет менять реализацию, не ломая внешний код. Публичный API остаётся стабильным.\n\n```ts\nclass Counter {\n  private count = 0;\n  increment() { this.count++; }\n}\n```\n\nТипичная ошибка — делать все поля публичными.',
      wrong: [
        'Создание нескольких экземпляров класса.',
        'Наследование свойств от родителя.',
        'Автоматическая сериализация.'
      ]
    },
    {
      id: 'ts-oop-12',
      subtopic: 'ООП — Модификаторы доступа',
      level: 'Middle',
      q: 'Для чего нужен protected?',
      a: 'Делает свойство или метод доступным внутри класса и его наследников, но не извне.',
      explain: 'protected полезен для базовых классов, где наследники должны использовать поле, но внешний код — нет.\n\n```ts\nclass Base { protected x = 1; }\nclass Child extends Base { get() { return this.x; } }\n```\n\nТипичная ошибка — путать protected и private.',
      wrong: [
        'Делает свойство доступным всем.',
        'Делает свойство readonly.',
        'Делает метод статическим.'
      ]
    },
    {
      id: 'ts-oop-13',
      subtopic: 'ООП — Модификаторы доступа',
      level: 'Middle+',
      q: 'Что такое статические методы и поля?',
      a: 'Принадлежат классу, а не экземплярам. Обращение через имя класса: MyClass.staticMethod(). Не наследуются экземплярами.',
      explain: 'Статические члены общие для всех экземпляров. Часто используются для фабрик и утилит.\n\n```ts\nclass MathUtil { static sum(a: number, b: number) { return a + b; } }\nMathUtil.sum(1, 2);\n```\n\nТипичная ошибка — обращаться к статике через экземпляр.',
      wrong: [
        'Принадлежат только экземплярам.',
        'Не могут содержать данных.',
        'Доступны только внутри класса.'
      ]
    },
    {
      id: 'ts-oop-14',
      subtopic: 'ООП — Модификаторы доступа',
      level: 'Senior',
      q: 'Назови основные принципы ООП.',
      a: 'Абстракция (выделение значимых характеристик), инкапсуляция (скрытие реализации), наследование (переиспользование через иерархию), полиморфизм (один интерфейс — разное поведение).',
      explain: 'Полиморфизм позволяет работать с разными классами через общий интерфейс. Наследование — не единственный способ переиспользования: композиция часто предпочтительнее.\n\n```ts\nabstract class Shape { abstract area(): number; }\nclass Circle extends Shape { area() { return 3.14; } }\n```\n\nТипичная ошибка — злоупотреблять наследованием вместо композиции.',
      wrong: [
        'Только инкапсуляция и наследование.',
        'Только полиморфизм.',
        'Только абстракция и композиция.'
      ]
    },

    // ================= ОБЩИЕ ТЕМЫ TS =================
    {
      id: 'ts-common-1',
      subtopic: 'Общие темы',
      level: 'Junior-',
      q: 'Какую проблему решает TypeScript?',
      a: 'Добавляет статическую типизацию в JavaScript. Позволяет ловить ошибки типов на этапе компиляции, а не в рантайме. Улучшает автодополнение и рефакторинг.',
      explain: 'TS не меняет рантайм: после компиляции получается обычный JS. Ошибки типов не доходят до продакшена.\n\n```ts\nfunction add(a: number, b: number) { return a + b; }\n// add("1", 2); // ошибка компиляции\n```\n\nТипичная ошибка — ждать от TS проверок в рантайме.',
      wrong: [
        'Ускоряет выполнение JS-кода.',
        'Заменяет JavaScript полностью.',
        'Уменьшает размер бандла.'
      ]
    },
    {
      id: 'ts-common-2',
      subtopic: 'Общие темы',
      level: 'Junior',
      q: 'Какие плюсы и минусы у TypeScript?',
      a: 'Плюсы: строгая типизация, ошибки на этапе компиляции, лучший рефакторинг, автодополнение. Минусы: требует компиляции, больше кода, порог входа, иногда усложняет простые задачи.',
      explain: 'Для небольших скриптов TS может быть избыточен. Для больших команд и проектов выгода очевидна.\n\n```ts\n// Плюс: автодополнение подсказывает поля user\nfunction greet(user: { name: string }) { return user.name; }\n```\n\nТипичная ошибка — оценивать TS только по объёму кода.',
      wrong: [
        'Только плюсы — минусов нет.',
        'Только минусы — медленный и сложный.',
        'Плюсы и минусы одинаковы, что у JS.'
      ]
    },
    {
      id: 'ts-common-3',
      subtopic: 'Общие темы',
      level: 'Junior+',
      q: 'Что TypeScript даёт «из коробки»?',
      a: 'Статическую типизацию, систему модулей, поддержку современных фич ECMAScript, совместимость с JavaScript, инструменты рефакторинга.',
      explain: 'TS транспилирует современный синтаксис в целевой JS. Можно использовать import/export, async/await и другие фичи.\n\n```ts\nimport { x } from "./module";\n```\n\nТипичная ошибка — думать, что TS заменяет Babel полностью.',
      wrong: [
        'Только типизацию.',
        'Только модули.',
        'Только транспиляцию в старый JS.'
      ]
    },
    {
      id: 'ts-common-4',
      subtopic: 'Общие темы',
      level: 'Junior+',
      q: 'Что такое статическая и динамическая типизация?',
      a: 'Статическая — типы проверяются на этапе компиляции (TypeScript, Java). Динамическая — типы определяются в рантайме (JavaScript, Python).',
      explain: 'В динамических языках ошибка типа проявится при выполнении. В статических — при компиляции.\n\n```ts\n// TS: ошибка до запуска\nconst x: number = "s";\n```\n\nТипичная ошибка — путать статическую типизацию с сильной.',
      wrong: [
        'Статическая — типы определяются в рантайме.',
        'Динамическая — типы проверяются компилятором.',
        'Это одно и то же.'
      ]
    },
    {
      id: 'ts-common-5',
      subtopic: 'Общие темы',
      level: 'Middle',
      q: 'Что такое TSDoc и зачем он нужен?',
      a: 'Стандарт документирования TypeScript-кода через комментарии. Позволяет генерировать документацию и подсказки в IDE. Аналог JSDoc, адаптированный под TS.',
      explain: 'Комментарии /** ... */ с тегами @param, @returns отображаются в подсказках IDE.\n\n```ts\n/** Складывает два числа. */\nfunction sum(a: number, b: number) { return a + b; }\n```\n\nТипичная ошибка — писать документацию без тегов.',
      wrong: [
        'Тестовый фреймворк для TS.',
        'Формат конфигурации tsconfig.',
        'Способ типизации функций.'
      ]
    },
    {
      id: 'ts-common-6',
      subtopic: 'Общие темы',
      level: 'Middle+',
      q: 'Какие есть аналоги TypeScript?',
      a: 'Flow (от Facebook, статическая типизация для JS), ReasonML, Elm, PureScript. Flow — ближайший аналог, но менее популярен.',
      explain: 'Flow использует комментарии-аннотации и работает через Babel. Elm и PureScript — отдельные языки, компилируемые в JS.\n\n```js\n// @flow\nfunction f(x: number): number { return x; }\n```\n\nТипичная ошибка — считать Flow полностью совместимым с TS.',
      wrong: [
        'Только Babel.',
        'Только Webpack.',
        'Только ESLint.'
      ]
    },
    {
      id: 'ts-common-7',
      subtopic: 'Общие темы',
      level: 'Senior',
      q: 'Что такое fp-ts / io-ts и какие проблемы решают?',
      a: 'fp-ts — библиотека функционального программирования для TS (Option, Either, Task). io-ts — runtime-валидация типов, проверка данных из внешних источников на соответствие TS-типам.',
      explain: 'fp-ts даёт абстракции для чистого функционального кода. io-ts закрывает разрыв между TS-типами и рантайм-данными.\n\n```ts\nimport * as t from "io-ts";\nconst User = t.type({ name: t.string });\nUser.decode(data); // Either<Errors, User>\n```\n\nТипичная ошибка — использовать io-ts без необходимости в сложных проектах.',
      wrong: [
        'Библиотеки для работы с формами.',
        'Библиотеки для тестирования.',
        'Утилиты для роутинга.'
      ]
    },
    {
      id: 'ts-common-8',
      subtopic: 'Общие темы',
      level: 'Senior',
      q: 'Назови основные монады в fp-ts.',
      a: 'Option (может быть Some или None — вместо null), Either (Left/Right — вместо throw), Task (асинхронная операция), Reader (зависимость от окружения), IO (синхронный side effect).',
      explain: 'Монады инкапсулируют контекст: отсутствие значения, ошибку, асинхронность. Они позволяют строить цепочки без вложенных проверок.\n\n```ts\nimport { option } from "fp-ts";\nconst x = option.some(1);\n```\n\nТипичная ошибка — использовать монады без понимания теории.',
      wrong: [
        'Только Option и Either.',
        'Только Task.',
        'Монады в fp-ts отсутствуют.'
      ]
    },
    {
      id: 'ts-common-9',
      subtopic: 'Общие темы',
      level: 'Senior',
      q: 'Зачем нужен runtime-валидатор типов вроде io-ts или zod?',
      a: 'TS-типы стираются в рантайме. Данные из API/форм/localStorage могут не соответствовать типам. Валидаторы проверяют структуру в рантайме и возвращают типизированный результат.',
      explain: 'Без валидации можно получить ошибку в глубине приложения. zod и io-ts дают и тип, и проверку из одной схемы.\n\n```ts\nimport { z } from "zod";\nconst User = z.object({ name: z.string() });\ntype User = z.infer<typeof User>;\n```\n\nТипичная ошибка — доверять данным из API без проверки.',
      wrong: [
        'Для ускорения компиляции.',
        'Для форматирования данных.',
        'Для сериализации в JSON.'
      ]
    }
  ]
});